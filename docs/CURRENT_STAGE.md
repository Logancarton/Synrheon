# Current Stage

## Active Boundary

**Ground 0 Integration Design**

The previous immediate target—direct E011-B integration of the narrow E011-A `EXPAND/STOP` policy—is paused for architectural reconciliation.

E011-A remains valid evidence that cognitive operation/target selection can be learned and transferred. HCT-1/HCT-2 now provide the stronger process-level foundation that should govern what gets integrated next.

## Ground 0

```text
large candidate / knowledge field
        ↓
learned context routing
        ↓
ordered reversible soft tapering
        ↓
small serious-candidate field
        ↓
state-dependent recurrent deliberation
        ↓
evidence / uncertainty
        ↓
commit | abstain | seek evidence | reopen
```

Learned pathway resistance remains optional pending stronger task-specific evidence.

## Scientific Status

```text
HCT-1     COMPLETE — 200 final worlds — all frozen gates passed
HCT-2     COMPLETE — 300 final worlds — all frozen gates passed
```

HCT-2 particularly supports:

```text
ordered sparse tapering as an efficiency mechanism
reversible suppression under context reversal
state-dependent recurrence as functionally distinct from tapering
explicit abstention when evidence is insufficient
```

Removing recurrence reduced good behavior to 45% despite 100% correct-candidate survival through tapering.

## Live Status

The running organism still has:

```text
observable runtime/UI               Verified
cognitive substrate                 Built
computational time                  Integrated
ordered experience + provenance     Integrated
E011-A policy                       Built experimentally
Ground 0 cognition                  NOT Integrated
```

Do not describe HCT-1/HCT-2 as live organism behavior.

## Immediate Next Task

Design the smallest production-compatible Ground 0 slice before adding more cognition.

It should define:

```text
1. legitimate live candidate field
2. learned context / operation selection interface
3. reversible taper checkpoint
4. recurrent-field checkpoint
5. bounded recurrent transition
6. commit / abstain / reopen state
7. backend-owned trace fields for UI observation
```

The synthetic HCT generator, hidden correct identity, and scientific scorer must remain outside production cognition.

## Completion Gate For This Stage

Ground 0 Integration Design is complete when the repository has one coherent, testable contract for the path above and the old E011-B assumptions have been reconciled rather than duplicated.

Only then should production integration begin.
